Algoritmo problem3

    // Variables Definition
    Definir number Como Entero

    // Data Input
    Imprimir "Give me a number?"
    Leer number

    // Data Process and output
    Si (number % 2 = 0) Entonces
       Imprimir "The [", number, "] number is an even number"
    FinSi

    Si ~(number % 2 = 0) Entonces
       Imprimir "The [", number, "] number is an odd number"
    FinSi


FinAlgoritmo